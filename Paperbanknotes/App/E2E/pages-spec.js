describe('AutoTrader pages check', function () {
    var scope, rootScope, ctrl, location;

    it('should GET home', function () {
        browser.get('http://localhost:5000/index.html#/');
    });
});